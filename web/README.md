# vn-admin-data — frontend (Nuxt 3)

This folder contains a Nuxt 3 frontend intended to replace the current `src/main/resources/static/index.html` Leaflet client.

Quick start:

1. cd web
2. npm install
3. npm run dev

The frontend calls the backend endpoints under `/api/map` as-is. Keep CORS or proxying in mind when running separately.
